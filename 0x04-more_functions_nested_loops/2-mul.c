#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: first input
 * @b: second input
 * Return: the result of the operation
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
